# Refinement Mode

## 七、精修 mode

用户让你"精修一篇已写好的文章"(不是重写,不是评审)时用这套工作流。

### 精修 vs 重写的边界

| 动作 | 精修 |
|------|------|
| 删冗余词/AI 填充词 | ✅ |
| 改一句话内的否定-纠正结构 | ✅ |
| 修格式问题(硬换行、中英文空格) | ✅ |
| 改单句表达(让它更直接) | ✅ |
| 删一个明显冗余的段落 | 谨慎 |
| 改段落顺序 | ❌ 属于重写 |
| 改章节结构 | ❌ 属于重写 |
| 改作者的技术判断 | ❌ 属于重写 |
| 补充新论点/新证据 | ❌ 属于重写 |

**原则**:精修只动词句,不动结构;只删冗余,不改事实;保留作者原有判断和表达习惯。

### 审查工作流

**Android 技术稿前置判断**:涉及 Android、Perfetto、性能、系统机制时,读取 `references/android-terminology.md`。`渲染链路`、`输入链路`、`Binder 调用链`、`透传`、`兜底`、`落盘` 等词如果能对应具体系统对象和观察证据,不要机械删;如果只是包装表达,再按黑话处理。

**活人感前置判断**:读取 `references/human-feel.md`。精修时不要把稿子改得更整齐、更像总结卡片;目标是保留作者原有判断和真实工作流痕迹,删掉意义通胀、顺手补分析、同义词轮换和格式用力过猛。

**第一步:填充词 grep 初筛**
```
真正|本质上|恰恰|其实|显然|众所周知|不言而喻
说实话|事实上|实际上|关键是|重要的是
值得注意|不得不说|需要指出
```

**第二步:逐处按"功能性 vs 填充性"判断**(标准见 3.3.6)——有对照对象、删掉会丢信息就保留;无对照对象只是强调就删。

**第三步:翻译腔动词 grep**(套路一,见 3.3.7)
```
接住|击穿|拆解|收口|不崩|不爆|打穿|打透|收紧|推开|扛住|立住|站稳|锋利|硬得|claim|catch
```
命中即判断"中文日常里会不会这样用",不会就换中性动词。

**第四步:翻译腔句式扫描**
- `X 很 Y:[内容]` / `更 Y 的 X:` 的起手式(套路二,3.3.9)——看冒号前是不是形容词评价,是的话删掉只留后半
- `X 的 Y 比 Z 更 W` 骨架(套路三,3.3.10)——抽象名词主语 + 评价词结尾,重写成具体主语 + 动作谓语
- `context|state|cache|claim|runtime` 等原样英文(套路四)——换成上下文/状态/缓存/断言/运行时

**第五步:格式扫描**——硬换行断名词、section 标题里的填充词、中英文空格不规范。

**第五步补充:基础校对扫描**——按 `references/copy-editing.md` 检查可见正文和机器可读内容边界,再处理直角引号、第二人称称呼、术语大小写、英文状态词误译、中文错词和数字表达。不要为了统一大小写误改代码、字段、URL、API 路径、trace 名称或线程名。

**第六步:逐一判断否定-纠正结构(3.3.1)**——读者没预设那个错误认知就删否定直接陈述 B;纠正真实常见误判可考虑保留,但检查全文计数。

**第七步:虚假引导语(3.3.5)和结构性元叙述(3.3.8)扫描**——找 `先记住` / `先别急着` / `这里要` / `本节只讲` / `放到下一节`,逐个判断"删掉是否影响读者理解"。

**第八步:活人感扫描**——按 `references/human-feel.md` 检查:
- 有没有「标志着/反映了/体现了/凸显了」这类意义通胀;
- 有没有事实后面顺手补一句没有新增证据的分析;
- 有没有为了避免重复把同一个技术对象换多个称呼;
- 有没有为了显得完整凑三项;
- 有没有每段加粗、标题包装过度、列表像目录。

处理原则:能删就删,能换成具体影响就换成具体影响;不要为了「更像人」新增口语化表演。

### 改动优先级

同样的 AI 味在不同位置严重性不同:

1. **最优先**:section 标题里的填充词(最刺眼,标题该最精练)。
2. **次优先**:每节开头第一句、结尾结论句(读者最容易盯到)。
3. **再次**:列表项开头(短句里填充词比例高)。
4. **中等**:段落首句(给段定调的位置)。
5. **最低**:长段落中间(填充词混在长句里不明显,硬改有时反而破坏节奏)。

时间有限时先扫 1-3 档,4-5 档列在备注里交回用户。

### 改动克制

- 改动数量和 AI 味浓度挂钩——原稿越干净改动越少。经过前序审查的稿子控制在 5-10 处以内。
- 不"为改而改":原句读起来自然、信息准确就别动。
- 不统一口吻:作者的表达习惯(偏口语/偏书面、短句/长句比例)是"活人感"的来源。
- 不把具体边角磨平:失败路径、犹豫、取舍、边界条件、读者反馈如果真实存在,优先保留。
- **改完自己回读一遍**,确认没因为删填充词而让句子反而不通。

### 翻译腔的根治方法(归化重写)

遇到一段 AI 生成的中文读着别扭但不好改单词的——圈不出具体病句,但通读起来就是不对——这是**骨架问题**,不是词句问题。单词级别的精修修不掉,必须重写。

方法:

1. 读完原段,确认意思(事实、判断、边界);
2. **把原段盖上**(物理遮住或关掉,不看着原文);
3. 用中文里本来会怎么说这件事,重新说一遍;
4. 再对一遍,确保没漏事实。

翻译学里这一步叫"归化"——钱钟书、傅雷这代译者花一辈子练的,就是从英文原文出发不经过英文句法,直接写出中文读者日常习惯的表达。AI 写中文时,骨架留在英文里,皮换成了中文词汇。要做的是把骨架也换了。

**什么时候一定要走这一步**:一段话圈不出具体病句但读两行就能认出是 AI——句句都能顺翻回英文——这是骨架翻译腔,只有重写能治。

**什么时候不该走**:原稿作者本来就是用中文骨架写的,只是偶尔有几个 AI 填充词——这种按正常精修走,别重写。重写风险是会丢掉作者的原意和原语气。
